# AI 填充功能改造说明

## 改动概述

将"AI 生成占位符"功能改造为"AI 填充内容"功能。用户上传资料文件，AI 从中提取信息自动填充到模板的占位符中。

## 主要改动

### 1. AIPlaceholderDialog.vue
- 标题改为"AI 智能填充内容"
- 提示文字改为"AI 将从资料文件中提取信息，自动填充到模板的占位符中"
- 默认模式改为 'mock'（方便测试）

### 2. Editor.vue - handleAIGenerate 方法
**改动前**：调用 `AIService.generatePlaceholders()` 生成占位符
**改动后**：调用 `AIService.extractDataForTemplate()` 提取数据填充占位符

工作流程：
1. 从资料文件中提取纯文本内容
2. 调用 AI 服务从资料中提取信息
3. 将 AI 返回的填充结果应用到表单
4. 显示填充成功的字段数量

### 3. aiService.js - extractDataForTemplate 方法

#### 核心优化：

**a) 加载占位符映射表**
- 从 `/src/data/placeholderMapping.json` 加载中文描述
- 用于帮助 AI 理解每个字段的含义

**b) 智能识别字段类型**
- 自动识别选择框字段（包含 _yes/_no/_m/_f/_has/_none 等后缀）
- 分类为文本字段和选择框字段

**c) 优化的 AI 提示词**
```
核心规则：
1. 文本字段：提取准确信息，保持格式
2. 选择框字段：根据资料判断，返回 ☑（选中）或 □（未选中）
3. 智能推理：根据上下文推理选择框状态
```

**d) 选择框值验证和修正**
- AI 返回后验证选择框字段的值
- 自动转换 true/false/"是"/"否" 等为 ☑ 或 □
- 确保选择框字段只包含正确的符号

**e) 增强的模拟模式**
- 支持更多字段类型识别（姓名、电话、地址、日期、民族、公司等）
- 智能处理选择框字段
- 根据关键词判断选择框状态

## 传递给 AI 的数据结构

```javascript
{
  // AI 配置
  mode: 'mock' | 'real',
  provider: 'kimi' | 'openai' | 'openrouter' | 'azure',
  apiKey: 'xxx',
  model: 'moonshot-v1-8k',
  
  // 资料文件内容（纯文本）
  dataFileContent: "张三，男，1990年5月15日出生...",
  
  // 占位符列表
  placeholders: [
    { name: 'p_name', description: '原告姓名' },
    { name: 'p_gender_m', description: '原告性别-男' },
    ...
  ],
  
  // 文档类型（可选）
  documentType: "劳动合同"
}
```

## AI 返回的数据格式

```json
{
  "p_name": "张三",
  "p_gender_m": "☑",
  "p_gender_f": "□",
  "p_birth_y": "1990",
  "p_birth_m": "5",
  "p_birth_d": "15",
  "p_phone": "13800138000",
  "p_ethnic": "汉族",
  "deposit_paid_yes": "☑",
  "deposit_paid_no": "□"
}
```

## 选择框字段处理

### 识别规则
字段名包含以下后缀会被识别为选择框：
- `_yes` / `_no` - 是/否
- `_m` / `_f` - 男/女
- `_male` / `_female` - 男/女
- `_has` / `_none` - 有/无

### 填充规则
- AI 根据资料内容判断该选项是否适用
- 适用返回 `☑`（选中）
- 不适用返回 `□`（未选中）

### 自动修正
系统会自动将以下值转换为正确格式：
- `true` / `"是"` / `"选中"` → `☑`
- `false` / `"否"` / `"未选中"` → `□`

## 测试建议

1. **模拟模式测试**：
   - 上传包含基本信息的 Word 文档（姓名、性别、电话等）
   - 点击"开始填充"
   - 检查文本字段和选择框字段是否正确填充

2. **真实 API 测试**：
   - 将 mode 改为 'real'
   - 配置正确的 API Key
   - 测试复杂文档的提取效果

3. **选择框测试**：
   - 重点测试性别、是否等选择框字段
   - 确认返回的是 ☑ 或 □ 符号
   - 测试互斥选项（如男/女只能选一个）

## 注意事项

1. 占位符映射表路径：`/src/data/placeholderMapping.json`
2. 选择框字段必须返回 `☑` 或 `□`，不能是其他值
3. AI 提示词中明确说明了选择框的填充规则
4. 系统会自动验证和修正选择框字段的值
5. 模拟模式使用正则表达式匹配，准确度有限，建议使用真实 API
