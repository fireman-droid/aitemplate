# 快速启动指南

## 功能概述

本系统现在支持：
1. ✅ **模板选择** - 从预置模板中选择
2. ✅ **自定义上传** - 上传自己的Word模板
3. ✅ **智能表单** - 根据模板自动生成表单
4. ✅ **AI填充** - 使用AI从资料中提取信息
5. ✅ **文档生成** - 一键生成填充后的Word文档

## 使用步骤

### 方式一：使用预置模板（推荐）

1. **启动应用**
   ```bash
   npm run dev
   ```

2. **选择模板**
   - 打开浏览器访问 http://localhost:5173
   - 在模板选择界面点击"房屋买卖合同纠纷"卡片

3. **填写表单**
   - 系统自动加载模板并提取占位符
   - 在表单中填写相关信息
   - 支持折叠/展开不同的分组

4. **AI智能填充（可选）**
   - 点击"AI 智能提取"按钮
   - 上传包含案件信息的资料文件
   - 配置AI参数（API Key等）
   - AI自动从资料中提取信息填充表单

5. **生成文档**
   - 点击"生成文档"按钮
   - 系统自动下载填充后的Word文档

### 方式二：上传自定义模板

1. **准备Word模板**
   - 创建Word文档
   - 在需要填充的位置使用 `{placeholder_name}` 格式
   - 例如：`原告姓名：{p_name}`

2. **上传模板**
   - 在模板选择界面点击"上传自定义模板"
   - 选择你的Word文件
   - 系统自动提取占位符

3. **填写和生成**
   - 后续步骤同方式一

## 模板文件说明

### 当前可用模板

#### 房屋买卖合同纠纷
- **位置**: `src/templates/house-sale-dispute/`
- **包含文件**:
  - `template.docx` - Word模板
  - `config.json` - 配置文件
  - `placeholders.json` - 占位符映射
- **字段分组**:
  - 当事人信息（原告、被告、第三人）
  - 诉讼请求与事实
  - 其他信息

### 占位符命名规则

- `p_` - 原告（自然人）
- `pc_` - 原告（法人）
- `pa_` - 原告代理人
- `d_` - 被告（自然人）
- `dc_` - 被告（法人）
- `t_` - 第三人（自然人）
- `tc_` - 第三人（法人）

### 字段类型

系统自动识别字段类型：

1. **选择框** - 包含 `_yes`/`_no`、`_m`/`_f` 等后缀
   - 显示为下拉选择框
   - 选项：☑ 选中 / □ 未选中

2. **大文本框** - 包含 `_detail`、`_content`、`_reason` 等关键词
   - 显示为多行文本框
   - 适合输入长文本

3. **普通文本框** - 其他字段
   - 显示为单行文本框

## 添加新模板

详细步骤请参考 [模板添加指南](src/templates/README.md)

简要步骤：
1. 在 `src/templates/` 下创建新文件夹
2. 添加 `template.docx`、`config.json`、`placeholders.json`
3. 在 `templateService.js` 中注册新模板

## 常见问题

### Q: 上传模板后看不到表单？
A: 这个bug已修复。如果仍然出现，请检查：
- Word文档中是否包含 `{placeholder}` 格式的占位符
- 浏览器控制台是否有错误信息

### Q: 占位符显示为英文名称而不是中文？
A: 需要在 `placeholders.json` 中添加对应的映射关系

### Q: AI填充不工作？
A: 请检查：
- AI配置是否正确（API Key、模型等）
- 资料文件是否包含相关信息
- 网络连接是否正常

### Q: 生成的Word文档中占位符没有被替换？
A: 请检查：
- 表单中是否填写了对应的字段
- 占位符名称是否与Word文档中完全一致（区分大小写）

### Q: 如何返回模板选择界面？
A: 刷新页面即可返回模板选择界面

## 技术栈

- **前端框架**: Vue 3 + Vite
- **UI组件**: Element Plus
- **Word处理**: docxtemplater + pizzip
- **状态管理**: Pinia
- **路由**: Vue Router

## 开发命令

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

## 浏览器支持

- Chrome (推荐)
- Firefox
- Edge
- Safari (部分功能可能受限)

## 下一步

- [ ] 添加更多模板（离婚纠纷、买卖合同纠纷等）
- [ ] 根据config.json动态生成表单分组
- [ ] 添加字段验证功能
- [ ] 优化移动端体验

## 反馈与支持

如有问题或建议，请查看：
- [实现总结](IMPLEMENTATION_SUMMARY.md)
- [模板添加指南](src/templates/README.md)
- [需求文档](.kiro/specs/multi-template-system/requirements.md)
