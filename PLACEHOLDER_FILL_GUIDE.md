# 占位符填充功能使用指南

## 功能说明

系统现在支持自动提取文档中的占位符，并提供表单化的填充界面。

## 使用流程

### 1. 准备模板文档

在 Word 文档中使用 `{占位符名称}` 格式标记需要填充的位置，例如：

```
姓名：{p_name}
性别：男 {p_gender_m} 女 {p_gender_f}
出生日期：{p_birth_y}年{p_birth_m}月{p_birth_d}日
```

### 2. 上传文档

- 点击上传区域，选择包含占位符的 Word 文档
- 系统会自动提取文档中所有 `{xxx}` 格式的占位符
- 提取完成后会在右侧显示占位符数量

### 3. 填充表单

右侧会显示所有提取到的占位符，并转换为可填充的表单：

- **文本字段**：如 `{p_name}` 显示为 "原告姓名: [输入框]"
- **选择字段**：如 `{p_gender_m}` 显示为 "原告性别-男: [选择框]"，可选择 ☑ 或 □
- **大文本字段**：如 `{facts_and_reasons}` 显示为多行文本框

### 4. 填充方式

#### 方式一：手动填充
直接在表单中输入或选择内容

#### 方式二：AI 填充（开发中）
点击"AI 生成"按钮，AI 会自动识别并填充字段

### 5. 应用到文档

- 填写完成后，点击"填充到文档"按钮
- 系统会将表单中的内容替换到文档的对应占位符位置
- 可以在左侧预览区查看填充后的效果

### 6. 下载文档

点击"下载文件"按钮，下载填充后的 Word 文档

## 占位符命名规则

为了让系统正确识别和映射，建议使用以下命名规则：

### 原告（自然人）
- `p_name` - 原告姓名
- `p_gender_m` / `p_gender_f` - 原告性别
- `p_birth_y` / `p_birth_m` / `p_birth_d` - 出生日期
- `p_phone` - 联系电话
- 等等...

### 被告（自然人）
- `d_name` - 被告姓名
- `d_gender_m` / `d_gender_f` - 被告性别
- 等等...

### 原告（法人）
- `pc_name` - 原告名称
- `pc_addr` - 原告住所地
- `pc_legal_rep` - 法定代表人
- 等等...

### 被告（法人）
- `dc_name` - 被告名称
- 等等...

### 第三人
- `t_name` - 第三人姓名
- `tc_name` - 第三人名称（法人）
- 等等...

### 代理人
- `pa_name` - 原告代理人姓名
- `da_name` - 被告代理人姓名
- 等等...

### 诉讼请求和事实
- `claim_content` - 诉讼请求内容
- `facts_and_reasons` - 事实与理由
- `total_amount` - 标的总额
- 等等...

完整的占位符映射表请查看 `PLACEHOLDER_MAPPING.json` 文件。

## 注意事项

1. **占位符格式**：必须使用 `{xxx}` 格式，花括号必须是英文字符
2. **占位符名称**：建议使用英文和下划线，避免使用中文和特殊字符
3. **重复占位符**：同一个占位符可以在文档中多次使用，填充时会全部替换
4. **选择框字段**：包含 `gender`、`yes`、`no` 等关键词的字段会自动识别为选择框
5. **大文本字段**：包含 `detail`、`content`、`reason`、`addr` 等关键词的字段会显示为多行文本框

## 示例

### 示例模板

```
原告：{p_name}，性别：男 {p_gender_m} 女 {p_gender_f}，
{p_birth_y}年{p_birth_m}月{p_birth_d}日出生，{p_ethnic}族，
{p_company}{p_position}，住所地：{p_reg_addr}，
联系电话：{p_phone}，身份证号码：{p_id_num}。

诉讼请求：
{claim_content}

事实与理由：
{facts_and_reasons}
```

### 填充后效果

```
原告：张三，性别：男 ☑ 女 □，
1990年1月15日出生，汉族，
北京XX科技有限公司软件工程师，住所地：北京市朝阳区XX街道XX号，
联系电话：138-1234-5678，身份证号码：110101199001011234。

诉讼请求：
一、判令被告支付原告货款人民币100,000元；
二、本案诉讼费用由被告承担。

事实与理由：
2023年5月1日，原告与被告签订《房屋买卖合同》...
```

## 技术支持

如有问题，请查看控制台日志或联系技术支持。
